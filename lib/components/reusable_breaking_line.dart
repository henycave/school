import 'package:flutter/material.dart';

class ReusableBreakingLine extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      height: 1,
      width: double.infinity,
    );
  }
}
